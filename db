package com.example.dats.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import com.example.dats.model.User;

public interface UserRepository extends JpaRepository<User, Integer> {
}
